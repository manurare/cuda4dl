This material has been compiled by Jose A. Iglesias <jalley@cvc.uab.es> for the Cuda for Deep Learning session

sudo apt-get install python-pycuda		# needed to run pycuda examples
sudo pip install torch torchvision		# needed to run pytorch examples
sudo apt-get install feh			# lightweight image viewer
